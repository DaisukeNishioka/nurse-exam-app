{
  "name": "line-liff-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "push": "git add . && git commit -m \"update\" && git push",
    "release": "npm run build && npm run deploy && npm run push"
  },
  "dependencies": {
    "@line/liff": "^2.27.1",
    "dotenv": "^17.2.2",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "gh-pages": "^6.3.0",
    "vite": "^7.1.2"
  }
}
